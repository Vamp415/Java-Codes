public class Division {
    public static void main(String[] args) {
        int dividend = 25, divisor = 5, quotient;
        quotient = dividend / divisor;
        System.out.println("Quotient of " + dividend + " divided by " + divisor + " is: " + quotient);
    }
}
