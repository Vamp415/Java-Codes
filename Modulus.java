public class Modulus {
    public static void main(String[] args) {
        int dividend = 25, divisor = 7, remainder;
        remainder = dividend % divisor;
        System.out.println("Remainder of " + dividend + " divided by " + divisor + " is: " + remainder);
    }
}
